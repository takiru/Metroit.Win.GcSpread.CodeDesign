# サポートするヘッダーの特定定義レイアウト
ColumnHeader セクションにて構成されます。
サポートされるセクション名、要素名は下記の通りとなります。

```json
{
  // 必須
  "Rows": [
    {
      // 必須
      "Columns": [
        {
          // 任意
          "Value": "タイトル3"
        }
        // ... ヘッダーの列の数分、複数指定
      ]
    }
    // ... ヘッダーの行の数分、複数指定
  ]
}
```
